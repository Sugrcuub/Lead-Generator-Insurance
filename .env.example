# Required
ADMIN_PASSWORD=change-me
SESSION_SECRET=replace-with-a-long-random-string

# Optional: enable email notifications when a lead is submitted
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
NOTIFY_TO=agent@example.com
NOTIFY_FROM=leads@yourdomain.com

# Port (platforms like Render set PORT automatically)
PORT=3000
